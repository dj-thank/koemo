# MoraWeave v0.1.0 — 日本語発話を「証拠の織物」として復元する

MoraWeaveは、Whisper系ASR、モーラ・音素・F0、公開語彙記憶、局所的な言語モデルを一つの答えへ早急に潰さず、**複数の独立した証拠流として保持し、互いの不一致を利用して曖昧区間だけを聴き直す**日本語文字起こし研究実装です。

公開日: 2026-08-28  
ライセンス: MIT（外部データ・モデルは各ライセンスに従う）  
正本ディレクトリ: [`moraweave/`](./moraweave/)

## 中心原則

```text
発話された内容 observedTranscript
        !=
読みやすく整えた内容 normalizedTranscript
```

LLMが自然な日本語を好んでも、音響的に存在しない助詞・単語・数値・固有名詞を観測文字列へ追加しません。言い淀み、言い直し、助詞誤り、語尾の崩れも、実際に発話された証拠として保存します。

## 独自機構

1. **Evidence Weave** — 音響、モーラ、語彙記憶、流暢性／言い淀みの4流を、候補ごとの信頼度と相互不一致から動的に融合します。
2. **Selective Re-listening** — 全音声を重いモデルで再処理せず、エントロピー、候補間距離、モーラ不整合が高い区間だけを切り出して高ビームで再デコードします。
3. **Grammar Honeytrap** — LLMだけが強く支持する「きれいすぎる候補」を検出し、音響・モーラ根拠がなければ観測結果から遠ざけます。
4. **Mora Shadow** — 漢字仮名交じり文字列と並行して、読み・モーラ列・音素列を影の表現として保持します。表記が違っても音が近い候補を救い、音が違う自然文を退けます。
5. **Hashed Public Memory** — JMdict、権利確認済み青空文庫、利用者が許可した語彙などを、原文を再配布しないハッシュ化n-gram／SQLite記憶へ変換します。
6. **Teacher Probability Cache** — ローカル教師LLMの候補確率を音声・文脈・候補集合のダイジェストでキャッシュし、同じ曖昧性へ毎回重い推論を行いません。
7. **Error Preservation Head** — 学習者の誤り、言い直し、フィラーを「消すべきノイズ」ではなく、保存対象として学習する補助ヘッドを持ちます。
8. **Rights Gate** — データを「学習可能」「派生特徴のみ利用可能」「再配布可能」に分離し、ライセンス不明な資産を自動的に停止します。

## 実装範囲

- モデル非依存のN-best証拠融合
- 4分岐動的ゲート
- モーラ／音素／F0／境界／誤り保存のマルチタスクWhisper拡張
- 曖昧区間検出と選択的再聴取計画
- ハッシュ化SQLite言語記憶
- ローカルLLM確率キャッシュ
- 公開データ権利台帳とHMAC話者分割
- CER、Kana-CER、MLER、数字誤り、言い淀み保存率、未根拠訂正率
- 観測文字列のSHA-256固定
- JSON、TXT、Markdown、SRT、VTT、TSV、JSONL出力

## 研究上の立場

Qwen3.8-Flash-NextのQSA、4分岐Gated Residual、N-gram Embedding、学習安定化の発想を、音声認識へそのまま移植したとは主張しません。MoraWeaveでは、それらを以下のように翻訳しています。

```text
QSA                     → 曖昧区間だけを選択するQuery-Selected Acoustic Memory
4-branch Gated Residual → 4つの独立証拠流の動的ゲート
N-gram Embedding        → 公開データ由来のハッシュ化語彙記憶
長文効率化               → 長時間音声の局所再聴取と確率キャッシュ
```

研究根拠、実装済み／未実装の境界、データライセンスは`moraweave/docs/`に固定しています。
