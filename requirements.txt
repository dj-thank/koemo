PySide6-Essentials
soundcard
numpy
keyboard
faster-whisper
ctranslate2
transformers
python-docx
sherpa-onnx

# Optional integrations: meeting detection, remote summary backends, model setup.
psutil
httpx
openai
huggingface_hub

# Optional: build native correction dictionaries from public Hugging Face datasets.
# Install requirements-tools.txt only when running scripts/build_native_corrections.py
# with --hf-dataset. Keeping datasets out of the app runtime avoids bundling
# pandas/pyarrow into the PyInstaller distribution.

# Windows-native live captions (Kanary-style low-latency preview).
winrt-Windows.Media.SpeechRecognition
winrt-Windows.Globalization
